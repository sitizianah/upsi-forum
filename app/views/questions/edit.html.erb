<% provide(:page_title, "Edit >#{@question.title }") %>

<div class="container">
   <div class="row justify-content-center" style="margin-top: 10vh">
      <div class="col-lg-10">
         <div class="card">
            <div class="card-body">
               <h1>Edit Question</h1>
               <%= render 'form', question: @question %>
            </div>
         </div>
      </div>
   </div>
</div>